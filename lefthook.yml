pre-commit:
  commands:
    lint-fix:
      glob: "*.{ts,tsx,json,css}"
      run: npx biome check --write {staged_files} && git add {staged_files}

pre-push:
  parallel: true
  commands:
    lint:
      run: pnpm lint
    build:
      run: pnpm build
    test:
      run: pnpm test
